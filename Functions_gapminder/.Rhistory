library(gapminder)
library(tidyverse)
calcGDP <- function(dat, year=NULL, country=NULL) {
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
head(calcGDP(gapminder, year=2007))
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)){
stop("year is not in the dataset.")
}
else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
gapminder(year=2017, country="Denmark")
head(gapminder(year=2017, country="Denmark"))
stop("year is not in the dataset.")
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(!is.numeric(year)) {
stop("year must be numeric")
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(!is.numeric(year)) {
stop("year must be numeric")
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
stop("year must be numeric.")
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
if(!is.numeric(year)) {
stop("year must be numeric.")
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
}
stopifnot(is.numeric(year)) {
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
calcGDP <- function(dat, year=NULL, country=NULL) {
stopifnot(is.numeric(year))
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)){
stop("value is not in the dataset")
}
else(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)){
stop("value is not in the dataset")
}
else(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)){
stop("value is not in the dataset")
}
else(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)){
stop("value is not in the")
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
stopifnot(!is.null(year)){
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
stop("year does not exist")
calcGDP <- function(dat, year=NULL, country=NULL) {
stopifnot(!is.null(year)){
stop("year does not exist")
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP <- function(dat, year=NULL, country=NULL) {
stopifnot(!is.null(year)){
stop("year does not exist")
}
if(!is.null(year)) {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)) {
stop("year does not exist")
}else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
else{dat <- dat[dat$year %in% year, ]
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)) {
stop("year does not exist")
}
else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP(gapminder, year=c(2030, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
if(any(is.null(year)) {
stop("year does not exist")
}
else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
head(calcGDP(gapminder, year=2007))
#the function does what i need it to do, except the stop message does not come
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(any(is.null(year))) {
stop("year does not exist")
}
else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
if(any(is.null(year))) {
warning("year does not exist")
}
else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)) {
warning("year does not exist")
}
else{dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
if(is.null(year)) {
warning("year does not exist")
}
else {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
else {
dat <- dat[dat$year %in% year, ]
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if(year=="NULL") {
stop("year does not exist")
}
else {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
if(year==na) {
stop("year does not exist")
}
calcGDP <- function(dat, year=NULL, country=NULL) {
if (!(country_in %in% gapminder$country)) stop("Country is not in the gapminder dataset.")
else {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(dat, year=NULL, country=NULL) {
stopifnot(is.null(year)) {
stop("year does not exist")
}
else {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
calcGDP <- function(dat, year=NULL, country=NULL) {
stopifnot(is.null(year)) {
stop("year does not exist")
}
else {
dat <- dat[dat$year %in% year, ]
}
if (!is.null(country)) {
dat <- dat[dat$country %in% country,]
}
gdp <- dat$pop * dat$gdpPercap
new <- cbind(dat, gdp=gdp)
return(new)
}
return(gdp)
library(gapminder)
library(tidyverse)
gapminder_gdp <- function(c,d) {
gapminder %>%
filter(country == c) %>%
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
ifelse(c(gapminder_df$year) == NULL, "NA", NA)
return(gdp)
}
calcGDP <- function(c,d) {
gapminder %>%
filter(country == c) %>%
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
ifelse(c(gapminder_df$year) == NULL, "NA", NA)
return(gdp)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
calcGDP <- function(c,d) {
gapminder %>%
filter(country == c) %>%
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
ifelse(c(gapminder_df$year) == NULL, "NA", NA)
return(gdp)
}
head(calcGDP(gapminder, year=2007))
calcGDP("Denmark", c(1967, 1977, 1987, 1997, 2007, 2017))
calcGDP("Denmark", c(1967, 1977, 1987, 1997, 2007, 2017))
options(scipen = 999)
calcGDP("Denmark", c(1967, 1977, 1987, 1997, 2007, 2017))
calcGDP("Denmark", c(1967, 1977, 1987, 1997, 2007, 2017))
#install.packages("gganimate")
#install.packages("gifski")
#install.packages("av")
#install.packages("gapminder")
library(tidyverse)
library(gganimate)
library(gifski)
library(av)
library(gapminder)
calcGDP("Denmark", c(1967, 1977, 1987, 1997, 2007, 2017))
calcGDP <- function(c,d) {
gapminder %>%
filter(country == c) %>%
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
ifelse(c(gapminder_df$year) == NULL, "NA", NA)
return(gdp)
}
calcGDP("Denmark", c(1967, 1977, 1987, 1997, 2007, 2017))
gapminder_gdp <- function(c,d) {
gapminder %>%
filter(country == c) %>%
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
ifelse(c(gapminder_df$year) == NULL, "NA", NA)
return(gdp)
}
gapminder_gdp("Denmark", c(1967,1977,1987,1997,2007,2017))
options(scipen = 999)
gapminder_gdp("Denmark", c(1967,1977,1987,1997,2007,2017))
gapminder_gdp <- function(c,d) {
gapminder %>%
filter(country == c) %>% #
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
ifelse(c(gapminder_df$year) == NULL, "NA", NA)
return(gdp)
}
gapminder_gdp("Denmark", c(1967,1977,1987,1997,2007,2017))
ifelse(is.null(c(gapminder_df$year)) == NULL, "NA", NA)
if(is.null(c(gapminder_df$year)) == NULL, "NA", NA)
gapminder_gdp <- function(c,d) {
gapminder %>%
filter(country == c) %>% #
filter(year %in% d) -> gapminder_df
gdp <- gapminder_df$gdpPercap*gapminder_df$pop
if(is.null(c(gapminder_df$year)) == NULL, "NA", NA)
else{
return(gdp)
}
}
calcGDP <- function(data, year=NULL, country=NULL) {
if(!is.null(year)) {
data <- data[data$year %in% year,]
}
if (!is.null(country)) {
data <- data[data$country %in% country,]
}
#The following variable gdp will then be the calculation of population * GDPpercapita
GDP <- data$pop * data$gdpPercap
#At last I will add GDP as a new column
new <- cbind(data, GDP=GDP)
return(new)
}
calcGDP(gapminder, year=c(1967, 1977, 1987, 1997, 2007, 2017), country="Denmark")
getwd()
